<!DOCTYPE html>
<html>
  <head>
    <%= typekit 'gxc4cnt' %>
     <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <title>Blogster | <%= yield(:title) %></title>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <!-- flash notices -->
    <% flash.each do |key, value| %>
    <%= content_tag :div, value.html_safe, class: "alert alert-#{key} text-center" %>
    <% end %>
    <!-- site navigation -->
    <nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Carl Weis</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to 'Tutorials', articles_path %></li>
          <!--   <li><a href="#">Projects</a></li>
            <li><a href="#">Videos</a></li>
            <li><a href="#">Hire Me</a></li> -->
          </ul>
          <ul class="nav navbar-nav pull-right">
            <li><%= @article != nil && @article.id ? link_to('Edit Article', edit_article_path(@article)) : '' %></li>
            <li><%= link_to 'Post Article', new_article_path, :class => 'navbar-btn-outline' %></li>
          </ul>
          </div><!--/.nav-collapse -->
        </div>
      </nav>
      <!-- main content -->
      <main id="main">
      <%= yield %>
      </main>
    </body>
  </html>